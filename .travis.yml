# sudo: false --> run dist image in docker container on shared host for faster startup (runs slower)
# sudo: required --> run dist image in VM on Google Compute Engine for slower startup (runs faster)
sudo: required
dist: trusty
os: linux

env:
  - SYSTEM_LEVEL=25 ABI="google_apis;arm64-v8a"
  - SYSTEM_LEVEL=25 ABI="google_apis;armeabi-v7a"
  - SYSTEM_LEVEL=24 ABI="google_apis;arm64-v8a"
  - SYSTEM_LEVEL=24 ABI="google_apis;armeabi-v7a"
  - SYSTEM_LEVEL=23 ABI="google_apis;armeabi-v7a"

  - SYSTEM_LEVEL=22 ABI="default;x86"
  - SYSTEM_LEVEL=22 ABI="default;x86_64"
  - SYSTEM_LEVEL=22 ABI="google_apis;armeabi-v7a"
  - SYSTEM_LEVEL=22 ABI="google_apis;x86"
  - SYSTEM_LEVEL=22 ABI="google_apis;x86_64"
  - SYSTEM_LEVEL=22 ABI="default;armeabi-v7a"

  - SYSTEM_LEVEL=21 ABI="default;armeabi-v7a"
  - SYSTEM_LEVEL=20 ABI="default;armeabi-v7a"
  - SYSTEM_LEVEL=19 ABI="default;armeabi-v7a"
  - SYSTEM_LEVEL=18 ABI="default;armeabi-v7a"
  - SYSTEM_LEVEL=17 ABI="default;armeabi-v7a"
  - SYSTEM_LEVEL=16 ABI="default;armeabi-v7a"
  - SYSTEM_LEVEL=15 ABI="default;armeabi-v7a"



matrix:
  allow_failures:
    - env: SYSTEM_LEVEL=20 ABI="default;armeabi-v7a"

    - env: SYSTEM_LEVEL=22 ABI="default;x86"
    - env: SYSTEM_LEVEL=22 ABI="default;x86_64"
    - env: SYSTEM_LEVEL=22 ABI="google_apis;x86"
    - env: SYSTEM_LEVEL=22 ABI="google_apis;x86_64"

    - env: SYSTEM_LEVEL=24 ABI="google_apis;arm64-v8a"
    - env: SYSTEM_LEVEL=25 ABI="google_apis;arm64-v8a"


language: android
android:
  components:
    - tools # revision 25.2.5 --> goes to 26.1.1 after calling sdkmanager tools again
    - platform-tools # revision 28.0.1

licenses:
    - 'android-sdk-license-.+'
before_install:
  # Silence warning.
  - touch ~/.android/repositories.cfg
  - sdkmanager --list
  # Install the rest of tools (e.g., avdmanager)
  - yes | sdkmanager tools # revision 26.1.1
  # Create and start emulator.
  - yes | sdkmanager "system-images;android-$SYSTEM_LEVEL;$ABI" >/dev/null
  - sdkmanager --list
  - echo no | avdmanager create avd --force -n test -k "system-images;android-$SYSTEM_LEVEL;$ABI"
  # use the absolute emulator path in case older version installed (on default path)
  - $ANDROID_HOME/emulator/emulator -avd test -no-audio -no-window &
before_script:
  # Wait for emulator to startup.
  - android-wait-for-emulator
  - adb shell input keyevent 82

script:
  - echo "Done."
