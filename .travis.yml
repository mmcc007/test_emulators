# sudo: false --> run in docker container on shared host instead of VM on Google Compute Engine for faster startup (runs slower)
# sudo: required --> run in VM on Google Compute Engine for slower startup (runs faster)
#sudo: required
os:
  - linux
#  - osx
#matrix:
#  allow_failures:
#    - os: osx
#  include:
#    - os: linux
#      dist: trusty
#      sudo: required
#    - os: osx
#      osx_image: xcode8.0

env:
  - EMULATOR_API_LEVEL=22 ANDROID_ABI="default;armeabi-v7a"
  - EMULATOR_API_LEVEL=23 ANDROID_ABI="default;armeabi-v7a"
#  global:
#    - EMULATOR_API_LEVEL=22
#    - ANDROID_ABI="default;armeabi-v7a"

language: android
android:
  components:
    - tools # revision 25.2.5 --> goes to 26.1.1 after calling sdkmanager tools again
    - platform-tools # revision 28.0.1

licenses:
    - 'android-sdk-license-.+'
before_install:
  # Silence warning.
  - touch ~/.android/repositories.cfg
  - sdkmanager --list
  # Install the rest of tools (e.g., avdmanager)
  - yes | sdkmanager tools # revision 26.1.1
  # Create and start emulator.
  - yes | sdkmanager "system-images;android-$EMULATOR_API_LEVEL;$ANDROID_ABI" >/dev/null
  - sdkmanager --list
  - echo no | avdmanager create avd --force -n test -k "system-images;android-$EMULATOR_API_LEVEL;$ANDROID_ABI"
  # use the absolute emulator path in case older version installed (on default path)
  - $ANDROID_HOME/emulator/emulator -avd test -no-audio -no-window &
before_script:
  # Wait for emulator to startup.
  - android-wait-for-emulator
  - adb shell input keyevent 82

script:
  - echo "Done."
